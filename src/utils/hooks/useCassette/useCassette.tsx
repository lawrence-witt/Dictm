/* import React from 'react';
import Cassette from './min/cassette.min.js';

const useCassette = () => {
    const cassette = React.useRef();

    React.useEffect(() => {
        cassette.current = new Cassette();

        console.log(cassette.current);
    }, []);
}

export default useCassette; */